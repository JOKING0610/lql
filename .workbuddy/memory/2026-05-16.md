# 2026-05-16

## 用户中心页面开发

- 创建了 `user-center.html` 独立用户中心页面
  - 未登录状态：显示登录/注册表单（Tab切换）
  - 已登录状态：显示用户卡片、播放统计、积分、功能菜单
  - 支持头像上传、退出登录确认弹窗
  - 支持深色/浅色主题切换
  - 功能菜单可跳转到主应用的个人主页或设置页
- 修改 `app.js` 中 `openLoginPage()` 函数，将跳转从 `login.html` 改为 `user-center.html`
- 在 `app.js` 初始化逻辑中添加 `navigateTo` localStorage 处理，支持从用户中心返回后跳转到指定页面

## 主应用增加用户中心入口

- 首页：在公告栏上方新增"用户中心"快捷入口卡片，显示登录状态描述
- 个人页面：在"登录/注册"按钮旁新增"前往用户中心"按钮
- 登录模态框：底部新增"前往用户中心"链接
- `app.js`：`updateLoginStatus()` 中增加首页用户中心描述文字和按钮文字的动态更新

## 用户中心主题适配完善

- 完善 `user-center.html` 主题系统，与主应用完全一致
  - 支持三种主题：浅色、深色、星空（带 canvas 流星动画）
  - 使用 `user_setting`/`guest_settings` 存储主题（与主应用 localStorage 结构一致）
  - 添加配色方案支持（浅色4种、深色4种）
  - 主题切换按钮组带滑动 pill 动画
  - 星空主题使用独立 canvas 实现星星和流星效果
  - 星空主题下界面元素透明化处理
- 新增功能：
  - 清除本地数据功能（保留登录信息）
  - 登录时自动迁移游客设置到用户设置
  - 返回首页快捷入口
  - Toast 提示替代 alert
  - 版本信息显示
- 优化播放统计读取逻辑：从 `user_plays`/`guest_settings.plays` 读取
- 修复星空主题切换闪烁问题：改为动画持续运行 + canvas 可见性控制

## 移除星座功能

- 移除星座选择和星座连线图案，星空主题只保留星星闪烁+流星效果
- App.jsx：移除 `constellationCanvas`/`constellationCanvasBili` 元素和星座选择下拉 UI
- app.js：删除 `starConstellations` 数组（12个星座数据）、`currentConstellationIndex`、`switchConstellation()`、`updateConstellationSelectSelected()`、`initConstellationSelect()`、`drawStarConstellation()`、`toStarCanvas()` 函数
- app.js：简化 `starAnimate()`（移除星座绘制阶段）、`startStarAnimation()`、`applyStarBg()`（移除星座组显示逻辑）
- app.js：`parseThemeString()` 中 star 不再带索引，`buildThemeString()` star 模式直接返回 `'star'`
- app.js：`initTheme()` 兼容旧版 `star0`~`star11` 统一映射为 `'star'`
- main.css：移除 `.star-constellation-label` 样式
- user-center.html：移除 `starConstellations` 数组，简化 `switchTheme()` 和 `parseThemeString()`

## CORS 音频代理修复

- 问题：酷我 CDN (lv-sycdn.kuwo.cn) 未返回 CORS 头，localhost:5173 播放音频被浏览器拦截
- `vite.config.js`：新增 `/api/kuwo-proxy` 代理规则，target 指向 `https://lv-sycdn.kuwo.cn`，自动替换 Referer/Origin
- `src/lib/app.js`：
  - 新增 `proxyAudioUrl(url)` 函数（仅 localhost 生效），将 kuwo CDN URL 重写为走 Vite 代理
  - 4 处 `audioElement.src` 赋值全部包上 `proxyAudioUrl()`
  - 生产环境不受影响

## 积分记录 RPC 修复

- 问题：`get_user_points` RPC 返回 404（函数未部署到 Supabase），且前端参数名不匹配
- `user-center.html`：两处调用 `{ _token: sessionToken }` 改为 `{ _user_id: userId, _session_token: sessionToken }`，匹配 SQL 函数签名
- 用户需要在 Supabase SQL Editor 执行 `get_user_points` 函数定义才能生效
