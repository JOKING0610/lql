
## 2026-05-17

- 修复 `redeemMembership()` 兑换成功后未刷新积分记录的问题：在 `pointsData = null; loadUserStats();` 之后添加 `loadPointsHistory()` 调用，确保积分余额和记录列表同步刷新
- 修复用户中心播放历史卡片溢出屏幕问题：移除 `.play-history-item` 的 `margin: 0 -8px` 负 margin，改为 `width: 100%; padding: 12px 8px; box-sizing: border-box`；`.content-panel-card` 添加 `overflow: hidden; max-width: calc(100vw - var(--sidebar-width, 0px))`；`.main-content` 添加 `max-width: 100vw; box-sizing: border-box; overflow-x: hidden`；所有响应式断点的 `.content-panel-card` 补上 `max-width: 100vw`
- 修复右侧空白问题：`.content-panel-card` 从 `position: relative; left: -28px; right: -28px`（right无效）改为干净的 `margin-left: -28px; margin-right: -28px`；移除 `.main-content` 的 `overflow-x: hidden`（会裁剪卡片右边+触发Y轴scrollbar）；body 的 `overflow-x: hidden` 作为唯一防溢出防线；所有响应式断点统一改为负 margin 方案
